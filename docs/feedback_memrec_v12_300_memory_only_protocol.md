# FeedbackMemRec V1.2: Locked 300-user Memory Only Ablation

Locked before launch on 2026-08-26 UTC as a supplemental ablation. It does not
change the original Corrected, Closed-loop Read, or Full V1.2 protocol.

## Purpose

Measure the isolated effect of Stage-R consuming dynamic neighbor memories
written by the unchanged Stage-W path.

## Locked data and execution

- Users and order: `data/eval_user_samples/strict_books_v12_300_seed45.json`
- User-list SHA-256: `41debfbf74d3cf1ccbd0a2eeaa3bf7c1932bde43734967eb7b852094355c41f8`
- Seed: 45
- Warmup: one serial pass over all 300 users
- Test: one serial frozen-credit pass over the same 300 users
- Candidates: 10, generated by the unchanged deterministic protocol
- GPU/service: physical GPU 1, `http://127.0.0.1:8001/v1`
- Output: `outputs/feedback_memrec_v12_continuous_300_memory_only`

## Locked feature flags

```text
neighbor_memory_read.enabled=true
feedback_credit.enabled=false
feedback_credit.learn_credit=false
read_credit.enabled=false
write_gate.enabled=false
attribution.enabled=false
```

The Stage-R total budget remains 1800 tokens. Dynamic neighbor memory remains
limited to 64 tokens per memory and 512 tokens per episode. Static neighbor
IDs, order, and snippets are preserved. The configuration SHA-256 is
`c63890c02b9b2c252afa0209508d3cf6b077483049a220734b4a9a26e6162172`.

The process starts from a newly constructed `MemoryStorage` and empty
`EvidenceCreditStore`; it does not load or share writable state from any other
variant. A nonzero process or acceptance exit is retained without retry.

## Planned supplemental comparisons

1. Memory Only - Corrected
2. Closed-loop Read - Memory Only
3. Full V1.2 - Closed-loop Read

